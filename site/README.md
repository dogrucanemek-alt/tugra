# tugra-ai.com

Static one-pager. Copy **approved 2026-08-30** (`METIN_TASLAK.md`).
Any change to a sentence needs approval again.

```bash
cd site
npm run build
npm run preview
```

No analytics. No cookies. No blog. No pricing table and no "enterprise"
button: the pricing section names the paid work and gives an address, because
the paid layer (panel, witness, team) does not exist yet. A table would price
something we cannot deliver.

Contact `e.dogru@tugra-ai.com` is live (Hostinger MX + SPF, verified
2026-08-30). ⚠️ Do not put an address on the page that has no mailbox —
a quote request would vanish silently.
